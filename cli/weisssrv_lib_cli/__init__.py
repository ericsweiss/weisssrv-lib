"""weisssrv-new-project: scaffold a weisssrv cluster tenant repo.

Commands (see cli.main): rename, prune, wire, verify. Offline and
dependency-light — the only third-party dependency is ruamel.yaml (round-trip
YAML editing that preserves the scaffold's comments).
"""

__version__ = "0.1.0"
