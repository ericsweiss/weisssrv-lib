# Python dependencies baked into the molecule-ci image.
#
# Exact pins, not lower bounds: this file is COPYed and pip-installed by
# docker/molecule-ci/Dockerfile, so a rebuild must be reproducible and must never
# silently pull a new major. The claim is scoped to pip — the sibling
# requirements.yml pins galaxy collections as major-line RANGES because those
# specs are shared with galaxy.yml, and that file's header says why.
#
# Three pins below (ansible-lint, black, yamllint) are the molecule side of a
# parity contract with the ci/lint template defaults — see
# tests/test_lint_version_parity.py for the exact pairs. ansible-core is not in
# that contract: no template input pins it, ansible-lint pulls its own.
ansible==11.6.0
ansible-lint==25.12.2
# black is a transitive dep (ansible-lint's jinja rule imports it); the black
# 26.5.x wheels ship a broken mypyc extension that makes ansible-lint fail to
# load. Lift once 26.5.x is fixed.
black<26.5.0
molecule==26.4.0
molecule-plugins[docker]==25.8.12
yamllint==1.38.0
