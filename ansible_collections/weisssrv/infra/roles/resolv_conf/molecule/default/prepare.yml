---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    # No apt install in this scenario, so import the shared prepare-base
    # (systemd-wait + temp-dir) WITHOUT prepare-apt-disable.
    - name: Apply the shared systemd-wait + temp-dir prep
      ansible.builtin.import_tasks: ../../../../molecule-shared/tasks/prepare-base.yml

    # A prior converge or a base-image refresh can leave +i set; clearing it
    # keeps re-runs deterministic.
    - name: Strip immutable attr off /etc/resolv.conf if present
      ansible.builtin.raw: chattr -i /etc/resolv.conf 2>/dev/null || true
      changed_when: false
