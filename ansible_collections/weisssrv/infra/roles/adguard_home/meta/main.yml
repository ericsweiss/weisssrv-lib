---
galaxy_info:
  role_name: adguard_home
  namespace: weisssrv
  author: Eric Weiss
  description: AdGuard Home DNS filtering with API-based configuration management
  license: MIT
  min_ansible_version: "2.18"
  platforms:
    - name: Debian
      versions:
        - trixie

# Ordering is the playbook's job: the resolver behind adguard_home_upstream_dns
# must be up before this role's post-deploy DNS probe. That is
# weisssrv.infra.unbound at the default upstream, and nothing at a public one.
dependencies: []
