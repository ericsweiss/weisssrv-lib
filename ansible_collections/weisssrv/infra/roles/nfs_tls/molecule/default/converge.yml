---
# `nfs_tls_enabled: true` is set via inventory group_vars in molecule.yml,
# so each guarded task in the role actually executes.
- name: Converge
  hosts: all
  become: true
  roles:
    - role: weisssrv.infra.nfs_tls
