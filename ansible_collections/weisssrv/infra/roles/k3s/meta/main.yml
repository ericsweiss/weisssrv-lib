---
galaxy_info:
  role_name: k3s
  namespace: weisssrv
  author: Eric Weiss
  description: Kubernetes k3s cluster with kube-vip, persistent storage, and workload placement
  license: MIT
  min_ansible_version: "2.18"
  platforms:
    - name: Debian
      versions:
        - trixie

# base/qol/postfix_null_client are run explicitly by the caller's playbook, in its first
# play against the k3s group, so this role declares no meta dependency on base
# (declaring one here would run the full base role a second time per host).
dependencies: []
