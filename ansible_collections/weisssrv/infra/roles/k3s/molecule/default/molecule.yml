---
platforms:
  # Both platforms join k3s_servers (the k3s_server_group default) so the
  # kube-vip agreement assert reads a REAL, populated group. Without the group
  # the set is empty and the assert passes vacuously — which is what its
  # membership clause now refuses.
  - name: k3s-server-test
    groups:
      - k3s_servers
    image: ${MOLECULE_TEST_IMAGE:-registry.git.ericsweiss.com/eric/weisssrv-lib/molecule-test:latest}
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    stop_signal: SIGRTMIN+3
    tty: true
    capabilities:
      - SYS_ADMIN
    tmpfs:
      - /run
      - /tmp
    networks:
      - name: k3s-test-net
  # Second server exercising the JOIN branch of k3s-server-config.yaml.j2
  # (k3s_is_first_server: false in host_vars): server stanza instead of
  # cluster-init, and no kube-vip manifest (first-server only).
  - name: k3s-server-join-test
    groups:
      - k3s_servers
    image: ${MOLECULE_TEST_IMAGE:-registry.git.ericsweiss.com/eric/weisssrv-lib/molecule-test:latest}
    pre_build_image: true
    privileged: true
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    command: /lib/systemd/systemd
    stop_signal: SIGRTMIN+3
    tty: true
    capabilities:
      - SYS_ADMIN
    tmpfs:
      - /run
      - /tmp
    networks:
      - name: k3s-test-net
provisioner:
  inventory:
    host_vars:
      # Audit logging ON here and left at its default (off) on the join platform,
      # so this one scenario covers BOTH renders: verify.yml asserts the args +
      # policy file here, and asserts the disabled server emits NOTHING there
      # (the byte-parity guarantee the server-config template is built around).
      k3s-server-test:
        k3s_audit_enabled: true
      k3s-server-join-test:
        k3s_is_first_server: false
    group_vars:
      all:
        k3s_version: v1.35.0+k3s3
        k3s_kube_vip_version: v1.0.4
        k3s_api_vip: 10.0.0.161
        k3s_token: test-token-for-molecule-testing
        k3s_role: server
        k3s_is_first_server: true
        k3s_kube_vip_interface: eth0
        k3s_labels:
          test: 'true'
        k3s_taints: []
        admin_user: root
        internal_domain: test.local
        base_skip_ssh_config: true
        base_skip_dns_config: true
        k3s_skip_install: true
        # Exercise the off-node etcd snapshot copy artifacts (script/service/timer).
        # The container-hostile steps (NFS mount, tlshd assert, timer start) are
        # molecule-notest-tagged; the file/template tasks render for verify.yml.
        k3s_etcd_snapshot_offnode_enabled: true
        # Exercise the metrics-server override render. Its live HelmChart probe
        # + assert are molecule-notest-tagged (no cluster in a container).
        k3s_metrics_server_override_enabled: true
