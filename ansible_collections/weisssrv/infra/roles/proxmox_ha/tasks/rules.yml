---
# Manage Proxmox HA node-affinity rules
#
# Note: This uses JSON output from ha-manager to avoid yq dependency.

- name: Check if ha-manager rules command exists
  ansible.builtin.command: ha-manager rules list --output-format=json
  register: proxmox_ha_rules_check
  changed_when: false
  failed_when: false
  check_mode: false  # Must run to determine current state
  tags: [proxmox_ha_rules]

- name: Fail if Proxmox version does not support HA rules
  ansible.builtin.fail:
    msg: >-
      HA rules command failed (rc={{ proxmox_ha_rules_check.rc }}).
      This could indicate: Proxmox version < 9 (rules require 9+),
      cluster not properly configured, or permission issues.
      Error: {{ proxmox_ha_rules_check.stderr | default('(no stderr)') }}
  when: proxmox_ha_rules_check.rc != 0
  tags: [proxmox_ha_rules]

- name: Parse current HA rules from JSON
  ansible.builtin.set_fact:
    # The JSON output has 'rule' attribute (not 'id') containing the rule name
    proxmox_ha_current_ha_rule_ids: "{{ (proxmox_ha_rules_check.stdout | from_json | default([])) | map(attribute='rule') | list }}"
    # Build a lookup dict keyed by rule name for drift detection
    proxmox_ha_current_ha_rules_lookup: >-
      {{ dict((proxmox_ha_rules_check.stdout | from_json | default([]))
         | map(attribute='rule')
         | zip(proxmox_ha_rules_check.stdout | from_json | default([]))) }}
  when: proxmox_ha_rules_check.rc == 0 and proxmox_ha_rules_check.stdout | length > 0
  tags: [proxmox_ha_rules]

- name: Set empty list if no rules exist
  ansible.builtin.set_fact:
    proxmox_ha_current_ha_rule_ids: []
    proxmox_ha_current_ha_rules_lookup: {}
  when: proxmox_ha_rules_check.rc != 0 or proxmox_ha_rules_check.stdout | length == 0 or proxmox_ha_rules_check.stdout == '[]'
  tags: [proxmox_ha_rules]

# The create/update tasks below gate on `item.type == 'node-affinity'`, so any
# other rule type (e.g. PVE9 resource-affinity) would otherwise be silently
# ignored — fail loud instead.
- name: Assert every HA rule is a supported type
  ansible.builtin.assert:
    that:
      - proxmox_ha_rules | default([]) | rejectattr('type', 'equalto', 'node-affinity') | list | length == 0
    fail_msg: >-
      Unsupported HA rule type(s) in proxmox_ha_rules:
      {{ proxmox_ha_rules | default([]) | rejectattr('type', 'equalto', 'node-affinity') | map(attribute='name') | join(', ') }}.
      This role only manages type: node-affinity rules — other types would be
      silently skipped, not applied.
  tags: [proxmox_ha_rules]

# Remove BEFORE add/update so replacing a rule (e.g. swapping the flat
# critical-services-no-nas for per-service affinity rules) never leaves a resource
# in two node-affinity rules mid-run, which Proxmox's rule feasibility check can
# reject. proxmox_ha_current_ha_rule_ids was captured above, before any mutation.
- name: Remove HA rules
  ansible.builtin.command: ha-manager rules remove {{ item.name }}
  when:
    - proxmox_ha_rules_check.rc == 0
    - item.name in (proxmox_ha_current_ha_rule_ids | default([]))
    - not (item.enabled | default(true))
  loop: "{{ proxmox_ha_rules | default([]) }}"
  register: proxmox_ha_rule_remove_result
  changed_when: proxmox_ha_rule_remove_result.rc == 0
  failed_when:
    - proxmox_ha_rule_remove_result.rc != 0
    - "'does not exist' not in (proxmox_ha_rule_remove_result.stderr | default(''))"
    - "'not found' not in (proxmox_ha_rule_remove_result.stderr | default(''))"
  tags: [proxmox_ha_rules]

- name: Create node-affinity HA rules
  ansible.builtin.command: >-
    ha-manager rules add node-affinity {{ item.name }}
    --resources {{ item.resources | join(',') }}
    --nodes {{ item.nodes | join(',') }}
    --strict {{ '1' if item.strict | default(false) else '0' }}
    {% if item.comment is defined %}--comment "{{ item.comment }}"{% endif %}
  when:
    - proxmox_ha_rules_check.rc == 0
    - item.name not in (proxmox_ha_current_ha_rule_ids | default([]))
    - item.type == 'node-affinity'
    - item.enabled | default(true)
  loop: "{{ proxmox_ha_rules | default([]) }}"
  register: proxmox_ha_rule_add_result
  changed_when: proxmox_ha_rule_add_result.rc == 0
  failed_when:
    - proxmox_ha_rule_add_result.rc != 0
    # Handle various Proxmox error messages for already-existing rules
    - "'already defined' not in (proxmox_ha_rule_add_result.stderr | default(''))"
    - "'already exists' not in (proxmox_ha_rule_add_result.stderr | default(''))"
  tags: [proxmox_ha_rules]

- name: Update existing node-affinity HA rules
  vars:
    # Current rule from the parsed JSON lookup.
    _current: "{{ proxmox_ha_current_ha_rules_lookup.get(item.name, {}) }}"
    # PVE9 `ha-manager rules list --output-format=json` returns `nodes` and
    # `resources` as HASHES, not comma strings:
    #   nodes:     {"pve-opt-01": {"priority": 1}, ...}
    #   resources: {"ct:150": {}, "vm:101": {}}
    # The old code split() these dicts as strings, so the comparison never matched
    # and the rule was re-`set` on every run. Normalize BOTH sides to a canonical
    # sorted string (node priority defaults to 0 on each side). The `is mapping`
    # guards keep this exception-safe if a future PVE returns the older string form
    # (it then falls back to the previous split() behaviour).
    _desired_nodes: "{{ item.nodes | map('regex_replace', '^([^:]+)$', '\\1:0') | sort | join(',') }}"
    _desired_resources: "{{ item.resources | sort | join(',') }}"
    _desired_strict: "{{ '1' if item.strict | default(false) else '0' }}"
    _desired_comment: "{{ item.comment | default('') }}"
    _current_nodes: >-
      {{ (_current.nodes | dict2items
            | map(attribute='key')
            | zip(_current.nodes | dict2items
                  | map(attribute='value.priority', default=0) | map('string'))
            | map('join', ':') | sort | join(','))
         if (_current.nodes is defined and _current.nodes is mapping)
         else (_current.nodes | default('') | string | split(',')
               | map('regex_replace', '^([^:]+)$', '\\1:0') | sort | join(',')) }}
    _current_resources: >-
      {{ (_current.resources | dict2items | map(attribute='key') | sort | join(','))
         if (_current.resources is defined and _current.resources is mapping)
         else (_current.resources | default('') | string | split(',') | sort | join(',')) }}
    _current_strict: "{{ _current.strict | default('0') | string }}"
    _current_comment: "{{ _current.comment | default('') }}"
    _has_drift: >-
      {{ _desired_nodes != _current_nodes or
         _desired_resources != _current_resources or
         _desired_strict != _current_strict or
         _desired_comment != _current_comment }}
    # `rules set` only updates supplied options, so a comment removed from
    # config must be cleared with an explicit --comment "" — otherwise the old
    # comment persists and the drift check re-fires every run without ever
    # converging (same pattern as replication.yml's _needs_comment_arg).
    _needs_comment_arg: "{{ (item.comment is defined) or (_current_comment | length > 0) }}"
    _comment_value: "{{ item.comment | default('') }}"
  ansible.builtin.command: >-
    ha-manager rules set node-affinity {{ item.name }}
    --resources {{ item.resources | join(',') }}
    --nodes {{ item.nodes | join(',') }}
    --strict {{ '1' if item.strict | default(false) else '0' }}
    {% if _needs_comment_arg %}--comment "{{ _comment_value }}"{% endif %}
  when:
    - proxmox_ha_rules_check.rc == 0
    - item.name in (proxmox_ha_current_ha_rule_ids | default([]))
    - item.type == 'node-affinity'
    - item.enabled | default(true)
    - _has_drift | bool  # Only update if there's actual drift
  loop: "{{ proxmox_ha_rules | default([]) }}"
  register: proxmox_ha_rule_update_result
  changed_when: proxmox_ha_rule_update_result.rc == 0
  tags: [proxmox_ha_rules]
