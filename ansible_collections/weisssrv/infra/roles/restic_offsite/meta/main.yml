---
galaxy_info:
  role_name: restic_offsite
  namespace: weisssrv
  author: Eric Weiss
  description: Nightly offsite backup to Backblaze B2 (restic + rclone) on pve-nas-01
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

# Dependencies are handled in the playbook (storage.yml / site.yml NAS play),
# not in meta, so the role unit-tests in isolation. It runs after nas_storage,
# which creates the archsync snapshots + the tank/backups/apps landing zone.
dependencies: []
