---
galaxy_info:
  role_name: restic_offsite
  namespace: weisssrv
  author: Eric Weiss
  description: Nightly offsite backup to Backblaze B2 (restic + rclone)
  license: MIT
  min_ansible_version: "2.18"
  platforms:
    - name: Debian
      versions:
        - trixie

# Dependencies stay in the caller's playbook so the role unit-tests in
# isolation. It must run after whatever creates the source snapshots it reads
# (weisssrv.infra.nas_storage's archive job in the reference deployment).
dependencies: []
