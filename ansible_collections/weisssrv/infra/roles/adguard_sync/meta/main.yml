---
galaxy_info:
  role_name: adguard_sync
  namespace: weisssrv
  author: Eric Weiss
  description: Automatic synchronization of AdGuard Home settings from primary to replica
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

# Ordering is the playbook's job: weisssrv.infra.adguard_home must have run on
# the primary before the first sync tick.
dependencies: []
