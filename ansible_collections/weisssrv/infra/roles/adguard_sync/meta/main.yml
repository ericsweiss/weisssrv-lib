---
galaxy_info:
  role_name: adguard_sync
  namespace: weisssrv
  author: Eric Weiss
  description: Automatic synchronization of AdGuard Home settings from primary to replica
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

dependencies:
  # In production, adguard_home should be installed first
  # In molecule testing, we skip this by not using this role's dependencies
  []
