---
galaxy_info:
  role_name: immich
  namespace: weisssrv
  author: Eric Weiss
  description: Immich photo-management stack (docker compose) on a dedicated Debian guest
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

# Dependencies stay in the playbook so the role can be unit-tested in isolation.
# compose_app (which itself includes docker_engine) is included from
# tasks/main.yml for the Docker install, compose unit, nginx flow and backup lib.
dependencies: []
