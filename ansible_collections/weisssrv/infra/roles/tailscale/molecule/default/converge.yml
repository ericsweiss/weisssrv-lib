---
# Skips `tailscale up` — no auth key and no network in CI; the scenario covers
# repo/pin/service/sysctl rendering only.
- name: Converge
  hosts: all
  become: true
  roles:
    - role: weisssrv.infra.tailscale
