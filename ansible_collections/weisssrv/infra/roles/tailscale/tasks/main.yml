---
# The role owns its own switch: a consumer addressing it by FQCN has no
# surrounding play to gate on (weisssrv gates in site.yml as well).
- name: Configure Tailscale
  ansible.builtin.include_tasks: configure.yml
  when: tailscale_enabled | bool
