---
# The role honours its own switch, so a consumer can list it unconditionally in
# a play and still disable it per host/group.
- name: Configure Tailscale
  ansible.builtin.include_tasks: configure.yml
  when: tailscale_enabled | bool
