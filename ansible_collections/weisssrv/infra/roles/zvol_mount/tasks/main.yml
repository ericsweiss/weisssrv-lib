---
# Mount block devices attached to the guest as extra SCSI disks.
#
# Input: zvol_mount_disks — dicts of name, mount_point, fstype, scsi_slot.
# /dev/sdX is NOT stable across reboots, so devices are addressed through
# zvol_mount_device_id_prefix + the slot. Slot 0 is the root disk, so extra
# disks start at 1 (which is also what the idx + 1 fallback produces).

# The role's one required input; the loop below is its first consumption, so
# without this the failure is a raw undefined-variable error.
- name: Assert the disk list is set
  ansible.builtin.assert:
    that:
      - zvol_mount_disks is defined
      - zvol_mount_disks | length > 0
    fail_msg: >-
      zvol_mount_disks is required: a list of {name, mount_point, fstype,
      scsi_slot} entries. It is conventionally the same list the guest's
      proxmox_vm_additional_disks names, so one inventory block feeds zvol
      creation on the host and mounting in the guest.
    quiet: true

- name: Reset zvol_mount_disk_devices for this invocation
  ansible.builtin.set_fact:
    zvol_mount_disk_devices: []

- name: Build disk device paths using stable QEMU SCSI IDs
  vars:
    # Use explicit scsi_slot if provided, else fall back to loop index + 1.
    # Explicit slots protect against SCSI reordering after VM hardware edits.
    scsi_slot: "{{ item.scsi_slot | default(idx + 1) }}"
    scsi_path: "{{ zvol_mount_device_id_prefix }}{{ scsi_slot }}"
    disk_entry:
      name: "{{ item.name }}"
      path: "{{ scsi_path }}"
      mount_point: "{{ item.mount_point }}"
      fstype: "{{ item.fstype }}"
  ansible.builtin.set_fact:
    zvol_mount_disk_devices: "{{ zvol_mount_disk_devices | default([]) + [disk_entry] }}"
  loop: "{{ zvol_mount_disks }}"
  loop_control:
    index_var: idx
    label: "{{ item.name }}"

- name: Check if additional disk device exists
  ansible.builtin.stat:
    path: "{{ item.path }}"
  loop: "{{ zvol_mount_disk_devices | default([]) }}"
  loop_control:
    label: "{{ item.name }} ({{ item.path }})"
  register: zvol_mount_disk_exists
  when: zvol_mount_disk_devices is defined

- name: Fail if disk device doesn't exist
  ansible.builtin.fail:
    msg: |
      Disk {{ item.item.name }} device {{ item.item.path }} does not exist!
      Check that the zvol is attached to the VM in Proxmox.
      Expected: {{ item.item.path }}
  loop: "{{ zvol_mount_disk_exists.results | default([]) }}"
  loop_control:
    label: "{{ item.item.name }}"
  when:
    - zvol_mount_disk_exists is defined
    - not item.stat.exists

# Use lsblk -no FSTYPE to detect existing filesystems. Unlike blkid, which can
# return non-zero for transient I/O errors (leading to destructive reformat),
# lsblk returns empty string only when the disk is truly unformatted.
- name: Detect filesystem type on additional disks
  ansible.builtin.command: "lsblk -no FSTYPE {{ item.path }}"
  loop: "{{ zvol_mount_disk_devices | default([]) }}"
  loop_control:
    label: "{{ item.name }} ({{ item.path }})"
  register: zvol_mount_disk_fstypes
  changed_when: false
  # Read-only probe — run it in --check too, or the registered results carry
  # no stdout and the downstream templating breaks under dry-runs.
  check_mode: false
  when: zvol_mount_disk_devices is defined

# Fail fast if a disk has an existing filesystem different from the expected type.
# Without this check, the next task would skip formatting (since the disk is "already formatted")
# but then try to mount it with the wrong fstype, which can fail at boot.
- name: Assert existing filesystem matches expected type (or disk is empty)
  ansible.builtin.assert:
    that:
      - (item.stdout | default('') | trim) in ['', item.item.fstype]
    fail_msg: >-
      Disk {{ item.item.name }} at {{ item.item.path }} has filesystem
      '{{ item.stdout | trim }}' but expected '{{ item.item.fstype }}'.
      Refusing to continue to avoid unsafe mounts.
    quiet: true
  loop: "{{ zvol_mount_disk_fstypes.results | default([]) }}"
  loop_control:
    label: "{{ item.item.name }} ({{ item.item.path }})"
  when: zvol_mount_disk_devices is defined

- name: Format additional disks only when no filesystem exists
  community.general.filesystem:
    fstype: "{{ item.item.fstype }}"
    dev: "{{ item.item.path }}"
  loop: "{{ zvol_mount_disk_fstypes.results | default([]) }}"
  loop_control:
    label: "{{ item.item.name }} ({{ item.item.path }})"
  when:
    - not ansible_check_mode  # Never format disks in check mode
    - zvol_mount_disk_devices is defined
    - (item.stdout | default('') | trim) == ''

- name: Get filesystem UUID for additional disks
  ansible.builtin.command: "blkid -s UUID -o value {{ item.path }}"
  loop: "{{ zvol_mount_disk_devices | default([]) }}"
  loop_control:
    label: "{{ item.name }} ({{ item.path }})"
  register: zvol_mount_disk_uuids
  changed_when: false
  # blkid fails on an unformatted disk (formatting is skipped in check mode);
  # allow it and let the UUID validation below handle the empty result.
  failed_when: false
  # Read-only probe — run it in --check too, so `item.stdout` exists for the
  # findmnt/mount templating below instead of failing a dry-run.
  check_mode: false
  when: zvol_mount_disk_devices is defined

- name: Validate all additional disks have UUIDs
  ansible.builtin.assert:
    that:
      - item.stdout | length > 0
    fail_msg: |
      Disk {{ item.item.name }} ({{ item.item.path }}) has no filesystem UUID!
      This usually means the disk is not attached or not formatted.
    quiet: true
  loop: "{{ zvol_mount_disk_uuids.results | default([]) }}"
  loop_control:
    label: "{{ item.item.name }}"
  when:
    - zvol_mount_disk_devices is defined
    - zvol_mount_disk_uuids is defined
    - not ansible_check_mode  # Skip validation in check mode (disks may not be formatted)

# Guard against duplicate ext4 UUIDs. The wrong-location findmnt check and
# the final mount both key on the blkid UUID, which is NOT unique across a
# zvol cloned or `zfs send | receive`d from a snapshot (the documented
# restore / re-provision pattern — a fresh zvol from a snapshot of a
# formatted disk inherits the source's filesystem UUID). Two attached disks
# sharing a UUID make `UUID=<x>` an ambiguous mount source: findmnt could
# report the wrong target and the mount could bind the wrong device. Fail
# loud here rather than silently mounting the wrong filesystem. Fix on the
# Proxmox host with `tune2fs -U random <dev>` (ext4) after a clone/restore.
- name: Assert additional-disk filesystem UUIDs are unique
  vars:
    _zvol_mount_uuids: >-
      {{ zvol_mount_disk_uuids.results
         | map(attribute='stdout')
         | map('default', '')
         | map('trim')
         | reject('equalto', '')
         | list }}
  ansible.builtin.assert:
    that:
      - _zvol_mount_uuids | length == (_zvol_mount_uuids | unique | length)
    fail_msg: >-
      Duplicate ext4 filesystem UUID across attached zvols: {{ _zvol_mount_uuids }}.
      Mounting by UUID is ambiguous when two disks share one (common after a
      zvol clone or `zfs send|receive` from a formatted snapshot). Regenerate
      the duplicate with `tune2fs -U random <dev>` on the Proxmox host before
      re-running.
    quiet: true
  when:
    - zvol_mount_disk_devices is defined
    - zvol_mount_disk_uuids is defined
    - not ansible_check_mode  # UUIDs may be absent in check mode (unformatted)

- name: Create mount points for additional disks
  ansible.builtin.file:
    path: "{{ item.mount_point }}"
    state: directory
    mode: "0755"
  loop: "{{ zvol_mount_disk_devices | default([]) }}"
  loop_control:
    label: "{{ item.name }}"
  when: zvol_mount_disk_devices is defined

- name: Check current mount status of additional disks
  ansible.builtin.shell: |
    findmnt -n -o TARGET "UUID={{ item.1.stdout | default('') | trim }}" 2>/dev/null || echo "not-mounted"
  args:
    executable: /bin/bash
  loop: "{{ zvol_mount_disk_devices | zip(zvol_mount_disk_uuids.results) | list }}"
  loop_control:
    label: "{{ item.0.name }}"
  register: zvol_mount_current_mounts
  changed_when: false
  # Read-only probe — run it in --check too (an empty UUID from a disk that
  # is still unformatted simply reports not-mounted).
  check_mode: false
  when:
    - zvol_mount_fix_wrong_locations | default(true)
    - zvol_mount_disk_devices is defined
    - zvol_mount_disk_uuids is defined

- name: Unmount disks mounted at wrong locations
  ansible.posix.mount:
    path: "{{ item.stdout | trim }}"
    state: unmounted
  loop: "{{ zvol_mount_current_mounts.results | default([]) }}"
  loop_control:
    label: "{{ item.item.0.name }}: {{ item.stdout | trim }} (should be {{ item.item.0.mount_point }})"
  when:
    - zvol_mount_fix_wrong_locations | default(true)
    - zvol_mount_current_mounts is defined
    - item.stdout | trim != "not-mounted"
    - item.stdout | trim != item.item.0.mount_point

- name: Remove incorrect fstab entries
  ansible.builtin.lineinfile:
    path: /etc/fstab
    regexp: "^UUID={{ item.item.1.stdout | trim }}\\s+"
    state: absent
  loop: "{{ zvol_mount_current_mounts.results | default([]) }}"
  loop_control:
    label: "{{ item.item.0.name }}"
  when:
    - zvol_mount_fix_wrong_locations | default(true)
    - zvol_mount_current_mounts is defined
    - item.stdout | trim != "not-mounted"
    - item.stdout | trim != item.item.0.mount_point

- name: Mount additional disks by UUID
  ansible.posix.mount:
    path: "{{ item.0.mount_point }}"
    src: "UUID={{ item.1.stdout | trim }}"
    fstype: "{{ item.0.fstype }}"
    # CRITICAL: Do NOT use nofail - if disk fails to mount, services must not start
    # (prevents writing to empty mountpoint on root disk, causing split-brain data)
    # x-systemd.device-timeout: fail fast if device doesn't appear (30s)
    opts: defaults,noatime,x-systemd.device-timeout=30
    state: mounted
  loop: "{{ zvol_mount_disk_devices | zip(zvol_mount_disk_uuids.results) | list }}"
  loop_control:
    label: "{{ item.0.name }} (UUID={{ item.1.stdout | default('pending') }}) -> {{ item.0.mount_point }}"
  when:
    - zvol_mount_disk_devices is defined
    - zvol_mount_disk_uuids is defined
    - not ansible_check_mode or (item.1.stdout | default('') | length > 0)
