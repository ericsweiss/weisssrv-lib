---
galaxy_info:
  role_name: docker_engine
  namespace: weisssrv
  author: Eric Weiss
  description: >-
    Shared pinned Docker Engine (CE) install: apt repo + held engine/plugins +
    journald daemon.json for the docker-compose app VMs
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - trixie

dependencies: []
