; Root pytest config so rootdir is the repo root. Without it cli/pyproject.toml
; is the only pytest config found and rootdir resolves to cli/, which prefixes
; every JUnit node ID with `cli/` — including tests that live in tests/.
[pytest]
testpaths = tests cli/tests
