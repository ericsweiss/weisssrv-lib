---
# Release list for scripts/validate-helm-values.py (`--releases`, default
# <repo-root>/helm-values-releases.yaml). Only value-heavy releases are worth
# rendering: each `helm template` is a network round-trip.
#
# A plain list is accepted too; the `releases:` key exists so the file can grow
# other top-level settings later.
releases:
  - name: traefik
    manifest: kubernetes/infrastructure/controllers/traefik/release.yaml
    chart: traefik
    repo_name: traefik
    repo_url: https://traefik.github.io/charts
  - name: kube-prometheus-stack
    manifest: kubernetes/infrastructure/observability/kube-prometheus-stack/release.yaml
    chart: kube-prometheus-stack
    repo_name: prometheus-community
    repo_url: https://prometheus-community.github.io/helm-charts
  - name: kured
    manifest: kubernetes/infrastructure/controllers/kured/release.yaml
    chart: kured
    repo_name: kured
    repo_url: https://kubereboot.github.io/charts
  - name: reloader
    manifest: kubernetes/infrastructure/controllers/reloader/release.yaml
    chart: reloader
    repo_name: stakater
    repo_url: https://stakater.github.io/stakater-charts
