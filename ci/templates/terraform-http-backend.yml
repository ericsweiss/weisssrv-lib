---
# GitLab-managed Terraform HTTP state backend fragment. Defines the hidden
# `.terraform-http-backend` job: TF_HTTP_* env pointing at this project's
# GitLab-hosted state, authenticated with the pipeline's CI_JOB_TOKEN. A
# consumer `extends: .terraform-http-backend`.
#
# Defaults target a state named `cloudflare` (weisssrv's default). A job that
# manages a DIFFERENT state overrides the three address vars per-job — exactly
# as weisssrv's tailscale-drift-plan / authentik-drift-plan do:
#
#   variables:
#     TF_HTTP_ADDRESS: "https://.../terraform/state/<name>"
#     TF_HTTP_LOCK_ADDRESS: "https://.../terraform/state/<name>/lock"
#     TF_HTTP_UNLOCK_ADDRESS: "https://.../terraform/state/<name>/lock"
.terraform-http-backend:
  variables:
    TF_HTTP_ADDRESS: "https://git.ericsweiss.com/api/v4/projects/${CI_PROJECT_ID}/terraform/state/cloudflare"
    TF_HTTP_LOCK_ADDRESS: "https://git.ericsweiss.com/api/v4/projects/${CI_PROJECT_ID}/terraform/state/cloudflare/lock"
    TF_HTTP_UNLOCK_ADDRESS: "https://git.ericsweiss.com/api/v4/projects/${CI_PROJECT_ID}/terraform/state/cloudflare/lock"
    TF_HTTP_USERNAME: "gitlab-ci-token"
    TF_HTTP_PASSWORD: "${CI_JOB_TOKEN}"
    TF_HTTP_LOCK_METHOD: "POST"
    TF_HTTP_UNLOCK_METHOD: "DELETE"
    TF_HTTP_RETRY_WAIT_MIN: "5"
